{
  "type": "module",
  "scripts": {
    "start": "npx vite public/",
    "build": "npx vite build",
    "lint": "npx prettier ./public --write",
    "install-dependencies": "npm i && cd public && npm i && cd .."
  },
  "dependencies": {
    "prettier": "3.3.3",
    "vite": "^5.4.7"
  }
}
